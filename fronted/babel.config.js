//o babel basicamente "compila" o codigo com o plugin da tecnologia js que voce ta usando (nesse caso o vue) e manda pro broswer o codigo otimizado (por alto eh isso)
module.exports = {
  presets: [
    '@vue/app'
  ]
}
